# include/

Project-wide public headers shared across `lib/` modules and
`src/main.cpp`. Currently holds `WifiCredentials.h.example` (committed
template) — copy it to `WifiCredentials.h` (gitignored) with real WiFi
credentials before building for real hardware; see
`docs/superpowers/specs/2026-07-25-v0.4-connectivity-design.md`.
